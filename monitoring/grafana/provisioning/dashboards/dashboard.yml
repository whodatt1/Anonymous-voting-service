apiVersion: 1

providers: # 대시보드를 어디서 읽어올지 정의하는 목록
  - name: vote-dashboards # 해당 프로바이더의 내부 식별 이름
    type: file # 파일 시스템에서 JSON을 읽어오는 방식
    options:
      path: /etc/grafana/dashboards # JSON 파일이 있는 컨테이너 내부 경로. docker-compose 볼륨 마운트에서
      # 해당 경로와 일치시켜야 함