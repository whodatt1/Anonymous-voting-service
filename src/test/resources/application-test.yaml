spring:
  # datasource / data.redis 접속 정보는 @ServiceConnection이 Testcontainers 컨테이너 기준으로 동적 주입하므로 명시하지 않음
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true